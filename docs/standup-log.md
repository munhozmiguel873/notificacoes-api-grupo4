# Daily Standup Log

## Template de Daily Standup
1. O que fiz desde a última aula?
2. O que vou fazer hoje?
3. Tem algo me impedindo?

## Registros

### 2026-05-14
- O que fiz desde a última aula:
  - Corrigi o controller de inscrições e o modelo `InscricaoModel`.
  - Ajustei `EventoController` e `ParticipanteController` para usar `try/catch` + `next(err)` e validações de ID.
  - Atualizei `README.md` e `docs/arquitetura.md` para refletir a implementação atual.
  - Limpei duplicatas em `src/routes/inscricaoRoutes.js` e adicionei middleware global de erro em `src/app.js`.
- O que vou fazer hoje:
  - Testar endpoints manualmente e validar se a documentação Swagger está completa.
  - Verificar se falta algum arquivo de configuração como `.env.example`.
- Tem algo me impedindo?:
  - Nenhum impedimento no momento.

### [data]
- O que fiz desde a última aula:
- O que vou fazer hoje:
- Tem algo me impedindo?:
